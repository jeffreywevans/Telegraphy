[tox]
minversion = 4.0
envlist = py314, py314-fast, py314-slow
labels =
    test = py314
    fast = py314-fast
    slow = py314-slow

[testenv]
description = Run the full project coverage workflow on Python 3.14.
package = editable
extras = dev
changedir = {toxworkdir}
setenv =
    COVERAGE_PROCESS_START = {toxinidir}/tox.ini
    COVERAGE_FILE = {toxworkdir}/.coverage
    TOX_PROJECT_ROOT = {toxinidir}
    PYTHONPATH = {toxinidir}{pathsep}{toxinidir}/telegraphy/scripts/cov_init
    # Intentionally inherited by all envs (e.g., py314-fast/py314-slow) via [testenv].
    PYTEST_DISABLE_PLUGIN_AUTOLOAD = 1
passenv =
    CI
    GITHUB_*
commands =
    python -m telegraphy.scripts.run_coverage_workflow

[testenv:py314-fast]
description = Run fast tests (exclude slow and integration markers).
commands =
    python -m pytest -m "not slow and not integration" tests

[testenv:py314-slow]
description = Run slow and integration test markers.
commands =
    python -m pytest -m "slow or integration" tests

[coverage:run]
source = telegraphy
relative_files = True
parallel = True
branch = True

[coverage:report]
show_missing = True
skip_covered = False
fail_under = 90
